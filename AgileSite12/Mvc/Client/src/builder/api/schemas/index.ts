/**
 * Provides normalization schemas for entities.
 * @module api/schemas
 */

import { areasSchema } from "./areas";
import { metadataSchema } from "./metadata";
import { pageSchema } from "./page";

export {
  areasSchema,
  pageSchema,
  metadataSchema,
};
