// Event names of events which are fired when a section is removed or mounted.
export const SECTION_REMOVE_EVENT: string = "kenticoSectionRemove";
export const SECTION_MOUNTED_EVENT: string = "kenticoSectionMounted";

// The width of a pop up container displayed after clicking on an add button, e.g. widget list.
export const POP_UP_CONTAINER_WIDTH: number = 300;

// The width of a section divider add button (button and dashed line).
export const SECTION_DIVIDER_ADD_BUTTON_OFFSET: number = 40;

// Time in ms after which an UI element should be hidden.
export const HIDE_ELEMENT_TIMEOUT: number = 250;

// Class of the global component wrapper host element.
export const GLOBAL_WRAPPER_ELEMENT_CLASS: string = "ktc-global-component";

// Height of builder dialog's header (px).
export const DIALOG_HEADER_HEIGHT: number = 36;

// Height of buider dialog's footer (px).
export const DIALOG_FOOTER_HEIGHT: number = 64;
