import { MediaFilesFormComponent } from "./media-files-form-component";

describe("kentico-media-files-form-component", () => {
  it("should build", () => {
    expect(new MediaFilesFormComponent()).toBeTruthy();
  });
});
