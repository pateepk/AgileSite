import { MediaLibrarySelector } from "./media-library-selector";

describe("media-library-selector", () => {
  it("should build", () => {
    expect(new MediaLibrarySelector()).toBeTruthy();
  });
});
