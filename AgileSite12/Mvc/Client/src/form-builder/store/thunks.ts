import { addSection, changeSection, moveSection, removeSection } from "./sections/thunks";
import { addWidget, moveWidget, removeWidget, setWidgetProperties } from "./widgets/thunks";

export {
  addWidget,
  moveWidget,
  removeWidget,
  setWidgetProperties,
  addSection,
  removeSection,
  changeSection,
  moveSection,
};
